---
"@marko/vite": minor
---

Add opt-in virtual entries and build identity for experimental persisted pages.
Update and persisted entry compiles share the build's compiler analysis cache;
non-persisted builds do not create the additional manifest state.
