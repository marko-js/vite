// Ordinary package with no `sideEffects` field, whose only export is used from
// a server block. Nothing references it in the client build, so it survives
// only because unlinked builds still treat it as impure.
(globalThis.__markoLoaded ??= []).push("impure-lib");

export function impureServerOnly() {
  return "impure-server-only";
}
